[project]
# Renamed from `synsc-context-proxy` (which still ships from the synsc-context
# repo for that product). Delphi gets its own slot so the two projects can
# version + publish independently.
name = "synsci-delphi-proxy"
version = "0.2.0"
description = "MCP proxy for Delphi — bridges stdio MCP clients (Claude Code, Cursor, Windsurf) to a Delphi HTTP API."
readme = {text = "MCP proxy for Delphi. Bridges stdio MCP clients (Claude Code, Cursor, Windsurf) to the Delphi HTTP API and mirrors the backend's current MCP tool profile.", content-type = "text/markdown"}
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [
    { name = "Aayam Bansal" }
]
keywords = ["mcp", "ai", "code-search", "research-papers", "huggingface", "datasets", "cursor", "claude", "semantic-search"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "mcp[cli]>=1.28.1",
    "httpx>=0.27.0",
    # Published security floors for MCP runtime transitives
    "click>=8.3.3",
    "cryptography>=48.0.1",
    "idna>=3.15",
    "pydantic-settings>=2.14.2",
    "pyjwt>=2.13.0",
    "python-multipart>=0.0.31",
    "starlette>=1.3.1",
]

[project.urls]
Repository = "https://github.com/synthetic-sciences/delphi"

[project.scripts]
synsci-delphi-proxy = "synsci_delphi_proxy:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["synsci_delphi_proxy"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
