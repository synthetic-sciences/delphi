export function ScopeStatement() {
  return (
    <aside className="my-12 border-y border-[var(--gold)] bg-[var(--gold-soft)] px-5 py-7 sm:px-7">
      <p className="eyebrow text-[var(--gold)]">What we can claim</p>
      <p className="mt-4 font-serif text-[24px] leading-8 text-[var(--fg-strong)] sm:text-[28px] sm:leading-9">
        Delphi is state of the art in this tested developer-work setting: a
        fixed Claude Opus 5 model on a 40-task DS-1000 development pilot.
      </p>
      <p className="mt-4 text-[14px] leading-7 text-[var(--fg-mute)]">
        We do not claim universal retrieval superiority, statistical
        significance from this pilot, or leadership on every retrieval metric.
      </p>
    </aside>
  );
}
